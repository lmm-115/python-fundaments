# Keyword: Word that have a function on a specific language

v = 13 

type(v) # type function tells you if a class variable is int or float
print(type(v))


# \n (new line) breaks the line; same as <br>
print("17 is an int \nAnd .5 is a float")

# \t (tab)
print("this tab like \t'this'")


# len returns the number of items of an object
print(len("hello"))

# len() with tuples, list and range
testList = []
print(testList, 'length is', len(testList))

testList = [1, 2, 3,]
print(testList, 'length is', len(testList))

testTuple = (1, 2, 3, 4, 5)
print(testTuple, 'length is', len(testTuple))

testRange = range(1, 10)
print('Length of', testRange, len(testRange))


